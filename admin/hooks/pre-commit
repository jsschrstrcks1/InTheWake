#!/bin/bash
#
# Pre-commit Security Hook - In the Wake
# Soli Deo Gloria
#
# This hook checks for security issues before allowing commits.
#

set -e

echo ""
echo "═══════════════════════════════════════════════════════"
echo "🔒 PRE-COMMIT SECURITY CHECK"
echo "═══════════════════════════════════════════════════════"
echo ""

# Get list of staged files
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM)

if [ -z "$STAGED_FILES" ]; then
    echo "✅ No files staged for commit"
    exit 0
fi

ERRORS=0
WARNINGS=0

# ============================================================
# CHECK 1: Forbidden File Types
# ============================================================
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📁 Checking for forbidden file types..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

FORBIDDEN_PATTERNS=(
    "\.env$"
    "\.env\."
    "\.pem$"
    "\.key$"
    "\.pfx$"
    "\.p12$"
    "\.sql$"
    "\.db$"
    "\.sqlite$"
    "credentials\."
    "secrets\."
    "\.kdbx$"
)

for pattern in "${FORBIDDEN_PATTERNS[@]}"; do
    MATCHES=$(echo "$STAGED_FILES" | grep -iE "$pattern" || true)
    if [ -n "$MATCHES" ]; then
        echo "❌ BLOCKED: Forbidden file type detected:"
        echo "$MATCHES" | sed 's/^/   /'
        ERRORS=$((ERRORS + 1))
    fi
done

if [ $ERRORS -eq 0 ]; then
    echo "✅ No forbidden file types found"
fi

# ============================================================
# CHECK 2: Secret Patterns in Staged Files
# ============================================================
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🔑 Scanning for hardcoded secrets..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

# Patterns that might indicate secrets (case-insensitive)
SECRET_PATTERNS=(
    "api[_-]?key\s*[:=]"
    "api[_-]?secret\s*[:=]"
    "password\s*[:=]\s*['\"][^'\"]{8,}"
    "secret[_-]?key\s*[:=]"
    "private[_-]?key\s*[:=]"
    "auth[_-]?token\s*[:=]"
    "access[_-]?token\s*[:=]"
    "-----BEGIN (RSA |EC |DSA |OPENSSH )?PRIVATE KEY-----"
    "-----BEGIN CERTIFICATE-----"
)

for file in $STAGED_FILES; do
    # Skip binary files and certain directories
    if [[ "$file" == *.png ]] || [[ "$file" == *.jpg ]] || [[ "$file" == *.webp ]] || \
       [[ "$file" == *.gif ]] || [[ "$file" == *.ico ]] || [[ "$file" == *.woff* ]] || \
       [[ "$file" == admin/claude/* ]]; then
        continue
    fi

    if [ -f "$file" ]; then
        for pattern in "${SECRET_PATTERNS[@]}"; do
            if git diff --cached "$file" | grep -qiE "$pattern" 2>/dev/null; then
                echo "⚠️  WARNING: Possible secret in $file"
                echo "   Pattern: $pattern"
                WARNINGS=$((WARNINGS + 1))
            fi
        done
    fi
done

if [ $WARNINGS -eq 0 ]; then
    echo "✅ No obvious secret patterns found"
fi

# ============================================================
# CHECK 3: DOM Danger Sinks (XSS Risk)
# ============================================================
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "🛡️  Checking for DOM XSS danger sinks..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

JS_FILES=$(echo "$STAGED_FILES" | grep -E "\.js$" || true)

if [ -n "$JS_FILES" ]; then
    for file in $JS_FILES; do
        if [ -f "$file" ]; then
            # Check for potentially dangerous patterns in NEW code
            DIFF_CONTENT=$(git diff --cached "$file")

            # innerHTML with potential user input (warn only)
            if echo "$DIFF_CONTENT" | grep -q "innerHTML\s*=" 2>/dev/null; then
                echo "⚠️  WARNING: innerHTML usage in $file"
                echo "   Ensure user input is properly escaped!"
                WARNINGS=$((WARNINGS + 1))
            fi

            # eval() usage
            if echo "$DIFF_CONTENT" | grep -qE "\beval\s*\(" 2>/dev/null; then
                echo "⚠️  WARNING: eval() usage in $file"
                echo "   Avoid eval() with user-controlled data!"
                WARNINGS=$((WARNINGS + 1))
            fi

            # document.write
            if echo "$DIFF_CONTENT" | grep -q "document\.write" 2>/dev/null; then
                echo "⚠️  WARNING: document.write() in $file"
                echo "   Consider safer alternatives!"
                WARNINGS=$((WARNINGS + 1))
            fi
        fi
    done

    if [ $WARNINGS -eq 0 ]; then
        echo "✅ No DOM XSS danger sinks in new code"
    fi
else
    echo "ℹ️  No JavaScript files in this commit"
fi

# ============================================================
# CHECK 4: Analytics Requirement (HTML files)
# ============================================================
echo ""
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "📊 Checking analytics requirement..."
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

HTML_FILES=$(echo "$STAGED_FILES" | grep -E "\.html$" | grep -v "admin/" || true)

MISSING_ANALYTICS=0
for file in $HTML_FILES; do
    if [ -f "$file" ]; then
        if ! grep -q "googletagmanager.com/gtag" "$file" 2>/dev/null; then
            echo "⚠️  Missing Google Analytics: $file"
            MISSING_ANALYTICS=$((MISSING_ANALYTICS + 1))
        fi
        if ! grep -q "cloud.umami.is/script.js" "$file" 2>/dev/null; then
            echo "⚠️  Missing Umami Analytics: $file"
            MISSING_ANALYTICS=$((MISSING_ANALYTICS + 1))
        fi
    fi
done

if [ $MISSING_ANALYTICS -eq 0 ]; then
    echo "✅ All HTML files have required analytics"
elif [ -n "$HTML_FILES" ]; then
    echo ""
    echo "   ℹ️  See CLAUDE.md Section 0 for required analytics snippets"
fi

# ============================================================
# SUMMARY
# ============================================================
echo ""
echo "═══════════════════════════════════════════════════════"
echo "📋 SECURITY CHECK SUMMARY"
echo "═══════════════════════════════════════════════════════"
echo ""
echo "   Errors:   $ERRORS"
echo "   Warnings: $WARNINGS"
echo ""

if [ $ERRORS -gt 0 ]; then
    echo "❌ COMMIT BLOCKED - Please fix the errors above"
    echo ""
    exit 1
fi

if [ $WARNINGS -gt 0 ]; then
    echo "⚠️  Warnings detected. Review before continuing."
    echo ""
    echo "   Press Enter to continue, or Ctrl+C to abort..."
    read -r
fi

echo "✅ Security checks passed"
echo ""
exit 0
