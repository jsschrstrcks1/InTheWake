# In The Wake — Modular Standards Superset v3.006

This bundle consolidates **every uploaded standard** (zips + markdown), preserving **every word** verbatim.
No edits, no deletions. Conflicts are **not resolved**; see `conflicts/conflicts_report.md` and decide.

## What’s inside

- `FULL_SUPERSET_v3.006.md` — complete concatenation of all sources with provenance blocks.
- `core/`, `dining/`, `logbook/`, `schemas/`, `sw/`, `ship-pages/`, `checklists/`
  - Each contains numbered files copied verbatim from sources.
- `conflicts/conflicts_report.md` — files with the same stem across sources.

## Versioning

This superset is labeled **v3.006** to align with site references. Original version strings within documents are preserved.
