# Ship Page Standards — v3.006

- Apply Every Page core.
- Hero image + compass + tagline.
- Swiper: resilience with primary + CDN fallback, keyboard nav, a11y labels.
- Stats card: SiteCache JSON load with local fallback JSON embedded in `<script type="application/json">`.
- Dining card: `venues.json` truth; alias support; expand venue slugs to objects.
- Logbook: personas carousel; keyboard left/right; anchors for state restore.
- Live Map: prefer AIS script; fallback to iframe after stall timeout.
- Related: sister class pills + class pills reorder.
