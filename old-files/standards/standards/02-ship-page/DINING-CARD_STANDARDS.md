# Dining / Venues Standards — v3.006
- Load `/assets/data/venues.json` via absolute same-origin URL.
- Expand ship.venues slugs to full objects using an index map.
- Alias array per ship for matching.
- Cache via SiteCache (7 days) with version pinning.
