/* Solo Cruising page — JavaScript: NONE.

   This page uses zero JavaScript by design.
   - The FAQ accordion is the native HTML <details>/<summary> element (no JS needed).
   - The only <script> tag in the full page is type="application/ld+json" — that is
     SEO/AEO structured data (JSON-LD), NOT executable JavaScript. It lives in its own
     deliverable: solo-cruising-schema.html.

   Nothing to add to WordPress for scripting. */
