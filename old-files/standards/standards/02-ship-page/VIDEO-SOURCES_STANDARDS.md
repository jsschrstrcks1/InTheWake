# Video Sources Standards — v3.006
- Normalize video JSON structures; accept arrays or grouped by categories.
- Robust YouTube ID parsing (id, video_id, youtube_id, URL variants).
- Deduplicate by `id` before rendering.
